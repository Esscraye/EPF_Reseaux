import Nav from './nav';
import Foot from './foot';

export default () => (
  `<header>
    ${Nav()}
  </header>
  <main>
    <div class="row">
    br
    </dib>
  </main>
  <footer>
    ${Foot()}
  </footer>`
);
